#include <stdio.h>
#include <stdint.h>
#include <stdlib.h>
#include <iostream>

using namespace std;

#ifdef richer
#elif defined(bellow) || defined(bellow) 
class Count
{
       void richer(count * c)(ref richer) {
           return Count(c*richer); 
       }
}
#endif // richer
