module matrix.gnu.bin.abc.array;

/**
In D, the [] for the array go on the left:
**/ 

import std.array;
import std.stdio;
import std.stdint;
import std.range;
import std.zlib;

class Array
{
   void array(const char Home, char c)(ref Richer) {
        int[3] *Array;                     // declares a pointer to an array of 3 ints
        long[] func(int i) { return i; }   // declares a function returning an array of
        
   }    
}

